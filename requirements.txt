--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
networkx>=3.1
matplotlib>=3.7.0

# Desktop app (PySide6 conversion)
PySide6>=6.7.0
pytest-qt>=4.4.0
pyinstaller>=6.10.0

# Real telemetry collection (replaces synthetic data generation)
psutil>=5.9.0
pywin32>=306; sys_platform == "win32"
# GPU utilisation, memory and temperature. Optional at runtime: a machine with
# no NVIDIA GPU records NULL for those channels rather than failing.
nvidia-ml-py>=12.0.0
