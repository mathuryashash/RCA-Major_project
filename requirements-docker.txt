# Docker-optimized requirements (CPU-only, runtime dependencies only)
# Full list in requirements.txt; this excludes unused packages (mlflow, dvc, etc.)

watchdog==4.0.0
PyYAML==6.0.1
drain3==0.9.11
pandas==2.2.1
numpy==1.26.4
scikit-learn==1.4.1.post1
fastapi==0.110.0
uvicorn==0.29.0
networkx==3.2.1
statsmodels==0.14.1
psycopg2-binary==2.9.9
jinja2==3.1.3
spacy==3.7.4
requests==2.31.0

# New modules: teammate dependencies
transformers==4.38.2
h5py==3.10.0
redis==5.0.2
boto3==1.34.52
pgmpy==0.1.25
causal-learn==0.1.3.8

# PyTorch CPU-only (no CUDA/NVIDIA libs — saves ~2GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.1+cpu
